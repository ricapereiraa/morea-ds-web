# Environment file for Docker Swarm (Morea IoT Data Collection)
# Copie e ajuste conforme necessário. Valores sensíveis não comitar.

# ===== Django / Aplicação =====
SECRET_KEY=n8fH4Kf3p9Wv2X1kT7QjL92bZRmC8fKq
DEBUG=False
ENVIRONMENT=PROD
# ALLOWED_HOSTS pode conter múltiplos hosts separados por vírgula
ALLOWED_HOSTS=192.168.1.122,192.168.1.80,localhost

# ===== Tokens =====
API_TOKEN=iot-token-4u23jf923jf23
INTERNAL_TOKEN=internal-token-99887766

# ===== Rede =====
# IP do host na rede Morea (onde estão os IoTs)
# IP do host na rede Morea (onde estão os IoTs)
MOREA_HOST_IP=192.168.1.136
# IP do manager (Fog network)
FOG_MANAGER_IP=192.168.1.80

# ===== Domínio e SSL =====
# Nota: para Traefik use apenas o hostname (sem http:// e sem porta).
# Você forneceu a URL completa; se usar Traefik, remova o prefixo e porta.
DOMAIN=morea.local
TRAEFIK_EMAIL=admin@example.com

# ===== Banco de Dados (PostgreSQL) =====
DBTYPE=PostgreSQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=morea
POSTGRES_PASSWORD=morea123
POSTGRES_DB=morea_db

# ===== Swarm / Hosts =====
SWARM_MANAGER_IP=192.168.1.80
IOT_NETWORK_IP=192.168.1.136

# ===== Integração =====
GITTOKEN=

# Hostname do node manager (para placement constraint)
SWARM_MANAGER_HOSTNAME=swarm-manager
