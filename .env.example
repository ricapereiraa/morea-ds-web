# Example environment variables for Morea
# Copy to .env and fill values before running docker-compose

# Django / general
SECRET_KEY=changeme_supersecret_key
DEBUG=True
ENVIRONMENT=DEV
ALLOWED_HOSTS=localhost,127.0.0.1

# Application settings
GITTOKEN=

# Database (leave DBTYPE empty or set to SQLite3 for local)
DBTYPE=SQLite3
DBNAME=
DBUSER=
DBPASSWORD=
DBHOST=
DBPORT=

# Traefik / Let's Encrypt
# DOMAIN is the public domain that will point to this host (e.g. example.com)
DOMAIN=yourdomain.example.com
TRAEFIK_EMAIL=you@example.com

# If you prefer to expose the app on a different internal port, change here
PORT=8000

# Notes:
# - Set DEBUG=False and a strong SECRET_KEY in production.
# - Ensure `DOMAIN` DNS points to the machine running Docker.
# - Create the ./traefik/letsencrypt/acme.json file and set its permissions to 600
#   (on Linux: `mkdir -p traefik/letsencrypt && touch traefik/letsencrypt/acme.json && chmod 600 traefik/letsencrypt/acme.json`).
SECRET_KEY=
DEBUG=

#DB-------------
#MySQL or SQLite3 (Case-sensitive)
DBTYPE=
#Your database address
DBHOST=
#Set your database configs
DBNAME=
DBUSER=
DBPASSWORD=
#default == 3306
DBPORT=
#Git Token for commits
GITTOKEN=
# DEV or PROD
ENVIRONMENT=
